/*
 * osada.c
 *
 *  Created on: 9/9/2016
 *      Author: utnso
 */
#include <stdint.h>
#include "osada.h"
#include <stdlib.h>
#include <stdio.h>

void inicializar(){

}

osada_file *leerArchivo(){
	osada_file *osadaFile = malloc(sizeof(osada_file));
	FILE * file= fopen("/home/utnso/tp-2016-2c-CompuMundoHiperMegaRed/cmhmr-osada/disco.bin", "rb");

	if (file != NULL) {
		fread(osadaFile, sizeof(osada_file), 1, file);
		fclose(file);
	}
	return osadaFile;
}

int devolverTamanioDelBloque(osada_file *osadaFile){
	return osadaFile->file_size;
}
